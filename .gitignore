# dependencies / build
node_modules/
dist/
.next/
coverage/
*.tsbuildinfo

# generated Prisma client (regenerated by `pnpm install` / `pnpm db:generate`)
apps/api/src/generated/

# environment and local secrets (never commit real values)
.env
.env.*
!.env.example

# brand-guard forbidden-term list stays outside the repository
.brand-guard-terms

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# logs
*.log
